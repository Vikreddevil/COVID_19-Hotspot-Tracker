<RelativeLayout android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        app:titleTextColor="#FFFFFF"
        android:background="@color/colorPrimary"
        android:layout_width="match_parent"
        app:theme="@style/ToolbarTheme"
        android:layout_height="?attr/actionBarSize">



    </androidx.appcompat.widget.Toolbar>
    <RelativeLayout
        android:layout_below="@id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/indicator_image_view"
            android:tint="@color/green"
            android:src="@drawable/ic_affected_area_indicator"
            android:layout_marginTop="30dp"
            android:layout_centerHorizontal="true"
            android:layout_width="100dp"
            android:layout_height="100dp"/>
        <TextView

            android:layout_below="@id/indicator_image_view"
            android:layout_marginTop="20dp"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:textStyle="bold"
            android:textColor="@color/dark_green"
            android:text="@string/green_zone"
            android:textSize="23sp"
            android:id="@+id/indicator_title_text_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            />


        <TextView

            android:justificationMode="inter_word"
            android:layout_marginTop="15dp"
            android:textColor="@color/black"
            android:layout_below="@id/indicator_title_text_view"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:text="@string/green_indicator_desc"
            android:textSize="16.5sp"
            android:id="@+id/indicator_desc_text_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            />
    </RelativeLayout>
<androidx.appcompat.widget.AppCompatButton
    android:layout_width="match_parent"
    android:layout_alignParentBottom="true"
    android:text="Stop Tracking"
    android:id="@+id/btn_stop_tracking"
    android:textSize="17.5sp"
    android:background="@color/colorPrimary"
    android:textColor="@color/full_white"
    android:textAllCaps="false"
    android:paddingBottom="10dp"
    android:paddingTop="10dp"
    android:layout_height="wrap_content"/>

</RelativeLayout>